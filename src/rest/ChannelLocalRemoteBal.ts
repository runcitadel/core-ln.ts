export interface ChannelLocalRemoteBalResponse {
  /** localBalance */
  localBalance?: number;
  /** remoteBalance */
  remoteBalance?: number;
  /** pendingBalance */
  pendingBalance?: number;
  /** inactiveBalance */
  inactiveBalance?: number;
}
